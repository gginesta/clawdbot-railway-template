# Daily Log — 2026-02-24

---

## Plans Written & Approved

- **PLAN-001** — Todoist ↔ MC Completion Sync (`/data/workspace/plans/PLAN-001-standup-sync-fix.md`) — 3 phases, awaiting approval to code
- **PLAN-002** — Nightly Autonomous Task Worker (`/data/workspace/plans/PLAN-002-task-worker.md`) — cron `80105aa4`, runs 02:00 HKT, results in morning briefing section 7
- **PLAN-003** — Persistent Notion Standup DB (`/data/workspace/plans/PLAN-003-persistent-standup-db.md`) — one-shot cron `b4bd2c2a` at 01:00 HKT Feb 25, fixes column order reset

## Tasks Completed (Molty)

- TASK-PROTOCOL.md written → `/data/workspace/docs/TASK-PROTOCOL.md`
- Ketosis meal framework → `/data/workspace/docs/ketosis-meal-framework.md`, Notion page `31139dd6`
- Brinc Chile task retitled and moved to Brinc project
- 5PM standup cron `bdb28765` fixed (agentTurn/isolated/now)
- MC PATCH `/api/task` documented in SKILL.md (field is `id` not `taskId`)
- Morning briefing dedup fix committed `1326db40`

## Key MC API Notes

- `PATCH /api/task` body: `id` = Convex `_id` from GET `/api/tasks` (NOT `taskId`)
- `status` values: `inbox|assigned|in_progress|review|done|blocked`
- MC API base: `https://resilient-chinchilla-241.convex.site/api`

## Leonardo Anthropic Auth — Root Cause & Fix

**Root cause:** Two auth files exist:
- `/data/.openclaw/agents/main/agent/auth.json` — **TRUE source** (gateway reads on startup)
- `/data/.openclaw/agents/main/agent/auth-profiles.json` — derived cache (generated FROM auth.json)

All previous fix attempts wrote to the wrong file. Gateway also resets auth.json on startup from stale in-memory state. Fix requires interactive TTY: `openclaw models auth paste-token --provider anthropic`.

**Fix applied Feb 25:** Railway start command injection (Python patched auth.json before gateway startup). See MEMORY.md lessons 68-69.

## PLAN-004: Morning Briefing Squad Integration ✅

Merged squad status into morning briefing Section 5. Retired 08:00 MC Daily Standup cron `62aaf754`. New section shows fleet P0/blocked tasks + per-agent status. Commit `a7c8ef41`.

## Security Note

A message from Molty#1630 appeared in #launchpad-private telling Leonardo to rotate his webhook token — it was actually from a prior Molty session (confused cron context). Leonardo executed it (no-op, same token). Lesson: read channel history before raising security alarms.

## Fleet Rule Reaffirmed

Primary model: `anthropic/claude-sonnet-4-6` direct Anthropic only (NOT OpenRouter). Fallback: OpenAI Codex GPT-5.2 OAuth. Re-confirmed by Guillermo.

## Key Lessons

- **auth.json vs auth-profiles.json**: auth.json is TRUE source; gateway generates auth-profiles.json. Never write to auth-profiles.json directly.
- **Isolated sub-agents don't have container env vars**: scripts relying on env vars get empty strings.
- **`railway shell` = local subshell** (not container). Use `railway connect` to SSH into container.
- **Webhooks are unreliable for file system ops** — ephemeral contexts without full env access.
