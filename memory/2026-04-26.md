# Memory Log — 2026-04-26

*Last updated: molty | 2026-04-26 23:00 HKT | Nightly curation*

---

## Fleet Rehab Session — Tokens, Infra, Dockerfile

### What happened
A full fleet recovery session ran today. Multiple infra items were fixed after the Apr 22 key-rotation fallout and extended downtime.

### Changes made

#### Tokens Rotated / Restored
- `RAILWAY_API_TOKEN` updated in Railway env. Project-scoped (not user-scoped) — `me {}` query blocked, `projects {}` works. TOOLS.md updated.
- `GITHUB_API_TOKEN` rotated and confirmed working. Remote URL updated to use correct var name.
- All Railway project IDs confirmed and documented in TOOLS.md.

#### Dockerfile (committed: `3d319e1f`)
- `@railway/cli` added to Dockerfile (so CLI survives redeploys without manual reinstall).
- `entrypoint.sh` updated with:
  - `git config --global --add safe.directory /data/workspace` — fixes "dubious ownership" errors in cron sessions
  - Auto-sets GitHub `backup` remote URL using `$GITHUB_API_TOKEN` on startup

#### MEMORY.md + TOOLS.md
- Fleet project IDs for all 6 Railway projects documented in TOOLS.md.
- Model chains standardized entry updated.
- Lessons added: project-scoped Railway tokens, redeploy required for env var changes, Railway CLI not persistent.

### Commits today (Apr 26)
1. `d5698176` — Railway/GitHub token lessons, fleet version status
2. `31222a85` — Fix GITHUB_API_TOKEN name in docs
3. `98ec0b10` — Fleet rehab docs: tokens, model chains, Railway project IDs
4. `3d319e1f` — Add Railway CLI to Dockerfile, fix entrypoint git safe.directory + GitHub remote

---

## Nightly Curation Cron

- **Status:** ✅ Ran successfully at 23:00 HKT
- MEMORY.md was previously noted as "nightly curation failing (3x timeout)" — today's run succeeded. The git safe.directory fix in entrypoint.sh may resolve future timeout issues.
- MEMORY.md size: 9,323 bytes (under 15KB cap ✅)

---

## Carried Forward (unresolved)

### Fleet v2026.4.23 Rebuild
- Dockerfile bumped to v2026.4.23 on Apr 25. Railway redeploy for all agents still pending.
- Molty currently on post-rebuild v2026.4.21 (cache issue on 4.23).
- Key fixes in 4.23: webchat blank bubble fix, memory QMD repair, block streaming dupes.

### Leonardo — Anthropic + xAI Tokens
- Still returning empty responses. Running on glm-5.1 fallback.
- Needs key verification in Railway env for Leonardo service.

### Leonardo + April — Version Confirmation
- Both were redeployed on Apr 24 (no-cache). Final version (`v2026.4.21`) confirmation pending next Guillermo session.

### BRI-44
- 16 Gmail drafts staged since Mar 18. Still blocked on Guillermo send confirmation.

### GPT-5.5 / Codex OAuth
- Pending Guillermo ChatGPT Plus/Pro subscription.

### WhatsApp SIM
- +34 677 43 78 34. Needs QR pairing when Guillermo is ready.
