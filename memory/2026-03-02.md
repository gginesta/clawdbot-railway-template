
## Memory Index Fix (11:00 HKT)

**Issue:** Weekly health check detected index corruption — 0 files indexed but 1,370 embeddings cached. Search returning nothing.

**Root cause:** Unknown — index database existed (121MB) but file/chunk mappings were empty.

**Fix applied:** `openclaw memory index --force` — now 369/369 files, 1,494 chunks indexed. Search working.

**Prevention:** Updated Weekly Memory Health Check cron to auto-fix this pattern:
- If files=0 but embeddings>0 → run `openclaw memory index --force` automatically
- Then re-check and report

**Lesson #93 added to MEMORY.md.**

---

## Evening Session (21:00-22:00 HKT)

### Standup Deduplication Fix
**Issue:** daily_standup.py was querying Notion DB for EVERY task insert to check duplicates — O(N²) API calls.

**Fix:** Commit `02b29303` — load all existing tasks ONCE into a dict cache at start, then O(1) lookups per task.
- `load_existing_tasks()`: paginated query, builds `{normalized_title: page_id}` dict
- `find_existing_task()`: simple dict lookup instead of API call

### MC API Transient Issue
Heartbeat at 7:55 PM reported "MC API unavailable" — checked at 9:06 PM and API was responding fine (200 on /api/tasks, /api/heartbeat). Transient Convex cold start or network blip. Self-resolved.

### Overnight Cron Fix
**Issue:** Molty overnight cron (80105aa4) showing "error" status. Last error: "Write to /data/shared/logs/overnight-molty-2026-03-02.md failed"

**Root cause:** `agentId` was empty string (`""`) in cron config.

**Fix:** `openclaw cron edit 80105aa4-... --agent main` — set agentId to "main". Cron now running properly.

### Overnight Task Plan (confirmed with Guillermo before bed)
- Raphael: 00:30 HKT
- Leonardo: 01:30 HKT  
- Molty: 03:00 HKT

Tonight's focus: Integrate usage report into heartbeat, enable MC_PASSWORD on Vercel, clean up ESLint warnings.

### Blocked Tasks Posted to #command-center
1. Cerebro — Stripe Live keys (Leonardo blocked, deadline Mar 14)
2. Brinc — Live proposal template (Raphael blocked on Guillermo deck)
