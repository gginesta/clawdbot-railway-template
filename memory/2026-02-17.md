# 2026-02-17

## Cron Fix & Config Cleanup
- All 11 cron jobs: `openrouter/google/gemini-2.5-flash` → `openrouter/anthropic/claude-3.5-haiku` (was unregistered)
- Gateway model fixes: heartbeat, subagent primary, and fallbacks aligned
- OpenClaw updated via `gateway update.run` (432 commits behind → current)
- Lesson 28: `agents.defaults.models` allowlist AND `models.providers.*.models` both required

## Memory System Pivot: QMD → OpenAI Builtin (20:00 HKT)

**Problem:** QMD hybrid search failed with OOM (~6GB needed on Railway), cmake issues, 30s timeouts. `extraPaths` config is QMD-only; OpenAI builtin only indexes `MEMORY.md` + `memory/**/*.md`.

**Solution (Option A1, Guillermo approved 20:53 HKT):**
- Move shared vault under `memory/vault/` on Molty only (auto-indexed by builtin)
- Other agents write to `/data/shared/memory-vault/` but don't index it
- Compartmentalization: Raphael→Brinc, Leonardo→Launchpad, Molty→all

**Execution:** Config simplified to 3 lines. Created `memory/vault/` with 297 copied files. Disabled QMD binary. Gate test passed (0.75 score).

**Lessons 31-34:**
- QMD reranker needs ~6GB RAM (OOM on Railway)
- OpenAI builtin: `MEMORY.md` + `memory/**/*.md` only
- Evaluate before brute-forcing (PPEE works)
- Simpler always wins over complex architecture

## Squad Feedback & A1.1 Update (21:13 HKT)

Raphael + Leonardo objected to Molty-only vault search = bottleneck.

**A1.1 Resolution:** Add `memory/squad/` to every agent (read-only mirror via Syncthing, auto-indexed). Contains standards + decisions + policies. Full vault stays Molty-only.

Guillermo: "They have my blessing. This is good team work."

**Pending:**
- Set up `memory/squad/` with curated content
- Configure Syncthing mirror to leads
- Weekly memory health cron (Leonardo requested)
- Config confirmations from Raphael + Leonardo
- Contribution flow end-to-end test
