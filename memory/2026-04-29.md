# Daily Log — 2026-04-29 (Wednesday)

## Codex OAuth Migration Correction

### What happened
- Guillermo ran Codex device-code OAuth locally from PowerShell.
- Initial assumption was wrong: `openclaw onboard --mode remote` does not store OAuth on the remote Railway gateway.
- It configures the local CLI remote target, then the device-code flow writes OAuth to Guillermo's local Windows auth store:
  - `C:\Users\guill\.openclaw\agents\main\agent\auth-profiles.json`
- Molty's Railway container still had an expired February Codex token, explaining ZAI fallback popups.

### Fix applied to Molty
- Guillermo pasted fresh local `auth-profiles.json` Codex OAuth profile.
- Copied fresh `openai-codex` OAuth profile into Molty container:
  - `/data/.openclaw/agents/main/agent/auth-profiles.json`
- Cleared stale Codex cooldown in:
  - `/data/.openclaw/agents/main/agent/auth-state.json`
- Verified via `session_status`: active model is `openai-codex/gpt-5.5 · oauth`.

### Documentation updated
- Corrected SOP: `memory/refs/codex-oauth-migration.md`
- Updated working memory: `MEMORY.md`

### Current state
- Molty: Codex OAuth verified active.
- Raphael: device-code flow completed locally; still needs fresh OAuth profile copied into Raphael's container and verified.
- Leonardo/April: paused until Molty/Raphael flow is proven end-to-end.

### Lesson
Do not claim Codex OAuth is active after device-code success alone. Verify the token inside the target container and confirm runtime model via `session_status` or `/status`.

### PowerShell switching lesson
- Local OpenClaw CLI has one active remote gateway target at a time.
- No new PowerShell process is required to move from Molty to Raphael; re-run `openclaw onboard --mode remote --remote-url ... --remote-token ... --flow manual --accept-risk` for the target agent.
- Then run `openclaw onboard --auth-choice openai-codex-device-code --accept-risk`.
- The resulting OAuth profile is still local to Windows; paste `Get-Content "$env:USERPROFILE\.openclaw\agents\main\agent\auth-profiles.json"` immediately so Molty can copy it into the correct container before moving to another agent.
- Do not reuse one refresh token across agents; generate/copy/verify one agent at a time.
- Molty's fresh token expires around 2026-05-09 09:31 HKT and should auto-refresh if the container copy is correct.

## Image Tool Fix
- Problem: screenshots arrived under `/data/.openclaw/media/inbound/`, but the image tool failed with `Failed to optimize image`.
- Root cause: OpenClaw's `media-understanding-core/image-ops.js` requires optional dependency `sharp`, but `sharp` was not installed under `/usr/local/lib/node_modules/openclaw/node_modules/`.
- Immediate fix: ran `npm install --prefix /usr/local/lib/node_modules/openclaw sharp`.
- Verification: direct `resizeToJpeg` succeeded on latest inbound PNG; `image` tool successfully read the screenshot text.
- Persistent fix: updated Dockerfile to install `sharp` under the OpenClaw package after global OpenClaw install.

## Memory Search Embeddings Investigation
- User asked whether latest OpenClaw update fixed `memory_search` and whether it could be fixed using Codex.
- Finding: Codex OAuth only covers chat/completions, not embedding requests. `memory_search` needs a separate embeddings provider.
- Previous config was pinned to OpenAI embeddings (`text-embedding-3-small`) with an invalid `OPENAI_API_KEY`, causing 401 errors.
- Attempted fix 1: switched `agents.defaults.memorySearch` to Gemini embeddings using `GEMINI_API_KEY` SecretRef. Runtime config validated and `openclaw memory status --deep --agent main` showed provider `gemini`, embeddings ready, but actual search hit Gemini quota exhaustion / 429 RESOURCE_EXHAUSTED.
- Attempted fix 2: installed `node-llama-cpp` and tried local embeddings (`embeddinggemma-300m-qat-Q8_0.gguf`, auto-downloaded ~329MB). Local status became ready, but full reindex was killed with exit 137 and search hung/failed on Railway resources. Not viable on current container size.
- Final state: reverted `memorySearch` config to Gemini provider so it will work once Gemini quota/billing is available. Current blocker is not Codex; it is embeddings capacity/credentials.
- Next sustainable options: (1) rotate/fix `OPENAI_API_KEY`, (2) enable paid Gemini quota, (3) add Voyage/Mistral embeddings key, or (4) provision more memory/CPU before using local embeddings.

## Raphael Codex Migration Complete
- Guillermo reported at 11:22 HKT that Raphael is fully migrated to Codex and working well.
- Updated `MEMORY.md` fleet table and Codex status to show Molty + Raphael on Codex, with Leonardo/April still pending.
- Updated `memory/refs/codex-oauth-migration.md` current verified/user-reported state.
- Note: Raphael status is user-reported; if debugging later, verify via Raphael `/status` or `session_status`.

## Fleet Infra Recovery Plan Started
- Guillermo asked to plan properly: fix memory first, then Tailscale/Raphael/Leonardo access, then Syncthing across fleet workspaces/shared drives/memory vault.
- Created plan: `/data/workspace/plans/fleet-infra-recovery-2026-04-29.md`.
- Live embedding tests after env updates:
  - OpenAI embeddings direct API: OK (`text-embedding-3-small`, 1536 dims)
  - Gemini embeddings direct API: OK (`gemini-embedding-001`, 3072 dims)
- Memory config moved to OpenAI embeddings primary with no fallback initially to avoid provider/key confusion; cache enabled.
- Verification: `memory_search` tool returned cited hits for `Codex OAuth migration correction`; CLI search also returned expected hits.
- Next gate: complete memory documentation, then begin Tailscale/access audit one agent at a time.

## Tailscale Diagnostic — Raphael/Leonardo
- 12:10 HKT cron check: Raphael replied to read-only diagnostic; Leonardo had not replied yet.
- Raphael findings: public `/setup/healthz` OK, but `supervisorctl` socket missing, no `tailscale`/`tailscaled` processes, `tailscale --socket=/tmp/tailscaled.sock status` fails, `/tmp/supervisord.log` missing. `TAILSCALE_AUTHKEY` present and `TAILSCALE_HOSTNAME=raphael-railway`.
- Root cause for Raphael: container is running OpenClaw directly, not supervisord, so Tailscale and Syncthing are not started. Source files confirm `entrypoint.sh` ends with `exec gosu openclaw node src/server.js` while Dockerfile copies supervisor config and uses `ENTRYPOINT ["./entrypoint.sh"]`.
- Safe local fix prepared: patched `entrypoint.sh` so it preserves `/data` + brew prep, then starts `/usr/bin/supervisord -c /etc/supervisor/supervisord.conf`; added `OPENCLAW_RUN_DIRECT=1` escape hatch for direct OpenClaw startup.
- Verification: `bash -n /data/workspace/entrypoint.sh` passed. Not pushed/deployed yet; fleet infra deployment requires explicit Guillermo approval.

## Raphael Recovery Incident
- 16:26 HKT Guillermo reported Raphael down after the Tailscale/supervisor recovery attempt.
- Railway latest deploy was SUCCESS and public UI responded, but logs showed agent runs failing: Codex network connection errors and direct Anthropic out-of-usage. `/setup/healthz` initially had `gatewayReachable=false`.
- Immediate recovery: set Railway env `OPENCLAW_PRIMARY_MODEL=zai/glm-5.1`, then patched Raphael's persisted live config via gateway `config.patch` because env vars did not override `/data/.openclaw/openclaw.json`.
- Redeployed Raphael. Verification: deployment `5aa9c005-adeb-40f1-880c-d72b2e5aecb5` SUCCESS; logs show `agent model: zai/glm-5.1`; `/setup/healthz` returned `gatewayReachable=true`; `channels.status` showed Discord + Telegram running; direct `chat.send` smoke test in session `agent:main:molty-smoke` returned assistant text `OK` using provider `zai`, model `glm-5.1`.
- Tailscale remains unresolved: logs still show `tailscale serve failed`. Do not continue Tailscale repair until Raphael responsiveness is stable and Guillermo approves a separate pass.
