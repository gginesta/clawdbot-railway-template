# OpenClaw Update — Feb 9, 2026
## 95263f4 → 6614c3f (32 commits)

### 🆕 New Features

1. **iOS Alpha Node App + Setup-Code Onboarding** (`6aedc54`, `730f86d`)
   - OpenClaw now has an iOS app (alpha) for device pairing
   - New phone control plugins (camera, screen, location)
   - Setup-code based onboarding flow
   - **For us:** Not relevant yet (Railway containers), but could be useful if Guillermo wants to pair his iPhone later

2. **Telegram Spoiler Tag Support** (`e02d144`)
   - Can now send spoiler-tagged messages on Telegram
   - **For us:** Nice for hiding sensitive info in Telegram alerts (e.g., tokens, passwords)

3. **Discord Forum Channel Thread-Create** (`9949f82`)
   - Can now create threads in Discord forum channels
   - **For us:** Could use for structured discussions in TMNT Squad server

4. **GitHub Copilot Models Added to xhigh List** (`744892d`)
   - Copilot models now recognized as high-tier for security
   - **For us:** Validates our Codex integration plan

5. **Exec Approvals: Monospace Forwarded Commands** (`ad8b839`)
   - Forwarded exec commands now render in monospace
   - **For us:** Better readability when reviewing commands in chat

### 🔧 Fixes (IMPORTANT)

6. **🔴 Cron: Share Isolated Announce Flow + Harden Scheduling/Delivery** (`8fae55e`)
   - Fixed the isolated cron delivery pipeline
   - **For us:** THIS IS THE FIX for our "cron delivery target is missing" errors! All our overnight crons were failing because of this bug. We already added `to: "1097408992"` as a workaround, but this fix should make delivery more robust.

7. **Context Overflow Compaction + Subagent Announce Improvements** (`191da1f`)
   - Better handling when context exceeds limits
   - Subagent announcements improved
   - **For us:** Direct improvement for our sub-agent workflows (Pikachu, standup agent, etc.)

8. **Config: Clamp maxTokens to contextWindow** (`eed580d`)
   - Prevents invalid configurations where maxTokens > contextWindow
   - **For us:** Safety net for our model configs

9. **Paths: Respect OPENCLAW_HOME for All Internal Path Resolution** (`db137dd`)
   - More consistent path handling
   - **For us:** Good for Railway containers where paths can be non-standard

10. **Memory/QMD: Warn When Scope Denies Search** (`7f7d49a`)
    - QMD now warns instead of silently failing when search scope is denied
    - **For us:** Better debugging for our QMD issues (OOM/SIGKILL)

11. **Refactor: Unify Peer Kind to ChatType** (`223eee0`)
    - Internal rename: `dm` → `direct`
    - **For us:** May need to check config if we reference `dm` anywhere

### 📚 Docs & Housekeeping
- Japanese locale support added (not relevant)
- Security & trust documentation added
- Centralized date/time formatting
- Various test and CI improvements

### ⚡ Action Items
- [x] Update applied and gateway restarted
- [ ] Monitor cron deliveries tonight — should work better with `8fae55e` fix
- [ ] Check if `dm` → `direct` rename affects our config
- [ ] Test Telegram spoiler tags for sensitive alerts
