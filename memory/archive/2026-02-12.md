# OpenClaw Update Analysis - 2026-02-12

**Old Version:** 2026.2.9 (96c46ed61)
**New Version:** 2026.2.10 (50a60b8be)

## Changelog Summary

**New features:**
*   `ClawDock` - shell docker helpers for OpenClaw development.
*   IRC — add first-class channel support.
*   `agentId` support to webhook mappings.
*   Gateway streaming thinking events and decoupling tool events from verbose level.
*   `--localTime` option to logs command for local timezone display.
*   LiteLLM provider integration.

**Fixes:**
*   Matrix media attachments.
*   Stripping reasoning tags and historical context from messaging.
*   Preventing `act:evaluate` hangs in browser tool.
*   Remapping session JSONL chunk line numbers.
*   Removing unsupported `include` param from Grok API calls.
*   Fixes related to pairing approval commands.
*   Enforcing embedding model token limits.
*   MacOS session preview repaint.
*   Memory-lancedb schema fixes.
*   UI form value coercion.
*   Handling xAI Responses API format in Grok provider.
*   Default-deny missing connect scopes in gateway.
*   Ignoring install scripts during plugin/hook install.
*   Not lowercasing Slack channel IDs.
*   Avoiding redacting maxTokens-like fields in config.
*   Dropping `logs --localTime` alias noise.
*   Using configured base URL for Ollama model discovery.

**Housekeeping:**
*   Changelog entries and format fixes.
*   Docker bash compatibility test.
*   Update Together default model.
*   Changelog notes for gateway streaming.
*   Skill name fixes and mandatory merge PR comments.
*   Docs updates for Terraform, pricing, mintlify skill, and gateway config.
*   Version bump to 2026.2.10.

## For Our Setup

**Action Items:**

1.  **Investigate `agentId` support in webhook mappings:** This is highly relevant for our multi-agent fleet (Molty, Raphael, Leonardo, etc.). Leveraging `agentId` in webhook mappings could improve inter-agent communication and message routing. Review `agent-link` skill for potential enhancements.
2.  **Utilize improved thinking/tool event streaming:** The decoupled thinking and tool events offer better insights into agent operations without verbose logging. We should explore routing these events to a dedicated monitoring channel or integrating them into `sessions_history` reviews for enhanced debugging and understanding of agent behavior.
3.  **Recommend `logs --localTime` to Guillermo:** Inform Guillermo about the new `--localTime` option for the `logs` command. This will allow him to view logs in HKT, which will significantly improve his experience with log analysis.
