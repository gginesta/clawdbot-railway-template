# OpenClaw Update Notes — 2026-02-08

## Version: 2026.2.6-3 → 2026.2.6-4
## Commits: a4d5c7f6 → e2dea268 (18 commits)

### Key Changes
1. **Agent Management RPC** (agents.create/update/delete) — #11045
2. **Context Overflow Recovery** — #11579
3. **WebClaw Compaction Divider** — #11341
4. **QMD Memory Hardening** — 5 commits (SQLite busy, timeouts, cache eviction)
5. **LAN IP WebSocket Fix** — #11448
6. **Multi-Agent Usage Tracking** — #11523
7. **.caf audio support** — #10982
8. **Pi-agent deps → 0.52.8**

### Impact on Our Setup
- Agent RPC: HIGH — can automate squad deployment
- Context overflow: CRITICAL — already hit this tonight, fix is automatic
- QMD fixes: CRITICAL — our CPU-only Railway setup benefits most
- Usage tracking: USEFUL — cross-agent cost visibility
- LAN fix: N/A — we use public Railway URLs

### Action Items
- [x] Test agents.create RPC for automated squad provisioning
  - ✅ WORKS! `openclaw agents add "name" --workspace /path --non-interactive --json`
  - Delete: `openclaw agents delete name --force --json`
  - Returns JSON with agentId, workspace, agentDir, bindings
  - Supports `--model`, `--bind channel[:accountId]`, `--agent-dir`
  - **Ready to script squad deployment** (Leonardo, Donatello, etc.)
- [x] Monitor QMD reliability
  - QMD index was 3 days stale — ran `qmd update` successfully
  - `qmd query` still gets SIGKILL'd (OOM on Railway CPU) — known issue
  - `qmd embed` running (41 docs, 596 chunks) — slow on CPU, no GPU
  - Gemini fallback for memory_search is working fine
  - **Verdict:** QMD hardening helps but CPU/RAM limits on Railway are the real bottleneck. Gemini embedding fallback is the practical path.
- [x] Check cross-agent usage in session_status
  - session_status shows current agent (main) usage fine
  - Cross-agent (Raphael) runs on separate Railway instance, so usage is per-instance not federated
  - **Not applicable** for our Railway multi-instance setup — would need shared billing dashboard
