# 2026-03-28 — Saturday

## Guillermo Location
- In **London** (GMT timezone). Left HK earlier in the week. Morning for him = ~8:45am GMT when he messaged.

## BuzzRounds — Party Games Hub (NEW PROJECT)
- **Concept:** Jackbox-inspired hub hosting multiple multiplayer browser games. No downloads, no signups.
- **Domain:** `buzzrounds.com` — purchased via Namecheap by Guillermo.
- **DNS configured:**
  - `@` → `76.76.21.21` (Vercel)
  - `www` → `cname.vercel-dns.com` (Vercel)
  - `tunes` → `hkhjmdcx.up.railway.app` (Railway custom domain — NOTE: this is the SECOND CNAME target after domain was deleted/recreated)
- **Vercel:** Project `prj_4lz2Cc50Ilao7XBEP9LA4dgmh6em`, created via API. Auto-deploys from `gginesta/buzzrounds` repo.
- **GitHub:** https://github.com/gginesta/buzzrounds
- **Tech:** Next.js 15 + TypeScript + Tailwind CSS. Light theme, gradient accents, SVG favicon.
- **Landing page:** 3 game cards — Tunes (live), YDKJ (coming soon), mystery game (TBD). Single-screen layout.

## Hitster → Tunes Rename (COMPLETED)
- Guillermo renamed to avoid Hitster trademark issues.
- **GitHub repo:** `gginesta/Hitster` → `gginesta/Tunes`
- **Railway:** Project + service renamed to "Tunes", domain `tunes.up.railway.app`
- **What I did:**
  1. Updated Railway env vars: `VITE_SERVER_URL` + `VITE_SPOTIFY_REDIRECT_URI` → `tunes.up.railway.app` (then later to `tunes.buzzrounds.com`)
  2. Updated GitHub Actions auto-merge workflow: `claude/hitster-spotify-game-*` → `claude/tunes-*`
  3. Full codebase rename: 32 files, all `@hitster/*` → `@tunes/*`, type aliases, UI text, localStorage keys, console logs, db filename. Commit `301ac19`.
  4. Guillermo updated Spotify Dashboard redirect URIs separately.
- **Claude Code PR #18:** Was struggling with the rename (timeouts, 503s). I did it directly. PR can be closed without merging.

## Custom Domain SSL Issue (IN PROGRESS)
- `tunes.buzzrounds.com` custom domain on Railway — SSL cert stuck on `CERTIFICATE_STATUS_TYPE_VALIDATING_OWNERSHIP`.
- Root cause: First CNAME target was `3s82uf9z.up.railway.app`. After I deleted and re-created the custom domain to reset cert, Railway assigned NEW target `hkhjmdcx.up.railway.app`. Updated DNS to match. Waiting for propagation + cert issuance.
- `tunes.up.railway.app` (Railway default domain) works fine as fallback.

## YDKJ (You Don't Know Jack)
- Second game in the BuzzRounds collection. Irreverent trivia with snarky host, trick questions, DisOrDat rounds, Jack Attack finale.
- Railway project `5cea0add-b247-4d06-b9fb-dc574d267e40` — no services deployed yet.
- GitHub: `gginesta/YDKJ`
- Future subdomain: `ydkj.buzzrounds.com` — DNS record to be added when service is deployed.

## Vercel Projects (updated)
| Project | URL | Purpose |
|---------|-----|---------|
| ginesta-site | https://ginesta.io | Guillermo family landing page |
| helmcl | https://helmcl.com | Helm Consulting placeholder |
| buzzrounds | https://buzzrounds.com | Party games hub (NEW) |
