# _tmp_unbrowse_example

This folder contains **fake / redacted** example output for the DIY Unbrowse API capture MVP.

It is meant to show the expected artifacts and folder structure without using real tokens.

Contents:

- `captures/` — example JSONL capture output
- `api-skills/example.com/` — example generated skill folder

Nothing here should be treated as a real API.
