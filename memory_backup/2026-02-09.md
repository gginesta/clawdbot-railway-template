# 2026-02-09 (Mon) — Running Todo / Log

## Today’s focus (live)
- [ ] Urgent open items (Todoist): Jinho recap email, 1Password setup, Discord slash allowlists
- [x] Remove tomorrow calendar blocks (conference day) + update standup logic to avoid re-blocking yesterday completions
- [ ] Agentation test on Proposal Studio (UI annotations → fix loop) — Phase 1 copy/paste
- [ ] Fix Syncthing desktop stuck at 50% + ensure laptop syncs
- [ ] Tailscale fix (desktop ↔ Railway) so we can do Agentation “watch mode” later

## Progress / Notes
- Created `x-reader` skill + Notion X Thread Repository DB and tested on real thread.
- Agentation: desktop chosen for running Agentation; Phase 1 copy/paste to OpenClaw; later MCP “watch mode” after Tailscale.
- Google Calendar: added 4 flights to Shenanigans + added 2 destination trip blocks (Cebu, Phuket) spanning exact depart→return-arrival times.
- Standup process update: Notion Task Review table columns should be `Context & Link` + `Comments` (Guillermo used Notes for comments today). Cron/template updated.
- Syncthing/Obsidian: canonical setup uses MV-* split folders under D:\GG\memory-vault\...; do NOT sync vault-root folder ID to Railway (hub doesn’t have it). Fix stuck states by removing overlapping folder IDs, restoring .stfolder, and ignoring .git/.obsidian.

## Syncthing investigation
- Root issue found: Desktop had two Syncthing folder IDs pointing to the same local path (D:\Molty) (`shared` and `rozuf-ajqgv`), causing churn/stuck state. Fix: remove the duplicate folder entry (keep files).
- Add ignore patterns for `.git/**` and `.obsidian/**` on vault folders to prevent endless out-of-sync.

- Railway (Molty-Railway) reports Desktop device is 100% complete for all folders (needBytes=0), including `shared` (~4.6GB).
- Desktop UI shows “Out of Sync Items” ~17,000+ mostly `.git/*` paths (git objects/index/logs). This commonly causes endless churn.
- Recommended fix: add ignore patterns for `.git/**` (+ common OS junk) on Desktop AND Laptop for affected folders, then rescan.
- No need for Desktop↔Laptop direct connection if both connect to Railway hub; but laptop must be connected to Railway (earlier screenshot showed laptop disconnected).
