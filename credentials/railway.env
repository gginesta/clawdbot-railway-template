# Railway CLI access
# Added: 2026-02-06
# NOTE: Railway CLI is logged in via `railway login` (OAuth session), NOT via token.
# The old RAILWAY_TOKEN was expired and caused "Invalid RAILWAY_TOKEN" errors
# that masked the working OAuth login. DO NOT set RAILWAY_TOKEN env var.
# If Railway CLI stops working, run: railway login
#
# Projects: Molty, raphael, webclaw, positive-consideration
# Account: guillermo.ginesta@gmail.com
